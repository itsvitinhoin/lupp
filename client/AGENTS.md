# AGENTS.md

AI agents working in this workspace: follow [`CLAUDE.md`](./CLAUDE.md) in this
directory (client-specific rules — design tokens, compiled widget outputs,
reusable primitives) and the repository root `CLAUDE.md` (architecture,
commands, verification rigs). Both are kept current; this file is a pointer so
tooling that looks for AGENTS.md finds the same instructions.
